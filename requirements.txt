# =============================================================
# Disertatie_AI_Platform - Dependinte runtime pentru aplicatia Streamlit
# =============================================================
# Acest fisier contine DOAR pachetele necesare rularii aplicatiei
# (inclusiv pe Streamlit Community Cloud). Setul complet folosit pentru
# antrenarea modelelor si notebook-uri (tensorflow, transformers, optuna,
# shap, jupyter etc.) se afla in requirements-dev.txt.

# --- Aplicatie web ---
streamlit>=1.30.0
streamlit-option-menu>=0.3.6

# --- Date si calcul numeric ---
pandas>=2.0.0
numpy>=1.24.0,<2
pyarrow>=14.0.0

# --- Optimizare neliniara (SLSQP) ---
scipy>=1.11.0

# --- Vizualizare interactiva ---
plotly>=5.17.0

# --- Configurare ---
PyYAML>=6.0
